# The real token lives at ~/.claude/channels/voice-channel/.env (mode 600) and is
# auto-generated on first run if unset. Copy that value into server/.env as
# VOICE_CHANNEL_TOKEN so the buddy server can post here.
VOICE_WEBHOOK_TOKEN=change-me-to-a-long-random-hex
